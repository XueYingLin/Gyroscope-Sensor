1. go to the maifests folder -> go to AndroidManifest.xml, add a line as below:
        <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="true" />

2. go to java folder -> go to the package "edu.sjsu.android.gyroscope", right click on it -> New -> Java Class
    -> type the class of hte class "GyroscopeSensor", and then GyroscopeSensor class created.

3. code at the Accelerometer class

4. code at the MainActivity class

5. run the program. after the emulator show, -> click on the 3 dots on the right side of the emulator
    -> click on the Virtual sensors on the Extended controls -> click on the Move
     -> move the x on the axis to see the color change green or yellow.


